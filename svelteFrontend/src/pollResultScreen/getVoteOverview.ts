import type { VoteOverview } from "./VoteOverview";

export async function getVoteOverview(pollId: string): Promise<VoteOverview> {
    //fetch all votes for a poll

    //fetch all votedOptions for every vote

    //count number of votes for every option

    //create VoteOverview by passing the name of the poll and the options with the number of votes

    return null;
}